TODO::
  -Update queries in term list when modified
  -Place queries name in queries button and update on change
  -change term field of GeneralTerm to var
  	-Add "<<" and ">>" buttons between queries and term on GUI to sync one with the other
	-Add popup "are you sure you want to override this term name (only the name will be lost, the definition will remain)" for "<<" button


What do we have to do when we boot up program??
	1) Establish some study set
		Load one from harddrive (most likely)
		Establish new one from Google




When we close
	1) Save to hard drive
	2) Upload to gDocs 
		(see jrtf)
		
https://developers.google.com/drive/v3/web/manage-uploads		

