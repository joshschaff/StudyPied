import content.publicAPI.StudyGuide
import javafx.scene.control.MenuBar

/**
 * TODO :: MOVE ALL OF THE FOLLOWING FROM STUDYPIED
 * Print Button
 * Category selector
 * Term counter (integrate into text of categoryt counter)
 */

class StudyPiedMenuBar(sg : StudyGuide) : MenuBar() {
    //init
}